<div id="new-item-container">
  <h2 id="forgot">Forgot your password?</h2>

  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <p>Email</p>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", style: 'background: #f5f5f5; border: solid black; opacity:0.9; filter:alpha(opacity=90); text-align: center;
      font-family: Cabin Sketch, cursive;'%>
    </div>

    <div class="actions">
      <%= f.submit "Send me reset password instructions", style: 'margin:15px; border: solid black; background: #f5f5f5; opacity:0.9; filter:alpha(opacity=90); text-align: center; 
      font-family: Cabin Sketch, cursive;' %>
    </div>
  <% end %>
  
  <%= render "devise/shared/links" %>
<div>
